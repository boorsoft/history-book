import 'package:flutter/material.dart';
import '../../style.dart';

class Paragraph15 extends StatelessWidget {
  @override 
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('§15.Кыргызстан в период "Развитого социализма" и "Перестройки" (1964-1990 гг.)'),
        ),
        body: 
        SingleChildScrollView(
          child:
        Container(
          padding: EdgeInsets.all(10.0),
          child:
          Column(
            children: [
              Text('lol'),
            ],
          ),
        ),
        ),
    );
  }
}