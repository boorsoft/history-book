import 'package:flutter/material.dart';
import '../../style.dart';

class Paragraph8 extends StatelessWidget {
  @override 
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('§8.Завершение процесса формирования кыргызской народности и его взаимоотношения с сопредельными народами'),
        ),
        body: 
        SingleChildScrollView(
          child:
        Container(
          padding: EdgeInsets.all(10.0),
          child:
          Column(
            children: [
              Text('lol'),
            ],
          ),
        ),
        ),
    );
  }
}