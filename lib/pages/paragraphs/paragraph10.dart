import 'package:flutter/material.dart';
import '../../style.dart';

class Paragraph10 extends StatelessWidget {
  @override 
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('§10.Присоединение Кыргызстана к России. Национально-освободительная борьба в Кыргызстане'),
        ),
        body: 
        SingleChildScrollView(
          child:
        Container(
          padding: EdgeInsets.all(10.0),
          child:
          Column(
            children: [
              Text('lol'),
            ],
          ),
        ),
        ),
    );
  }
}